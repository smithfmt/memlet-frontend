import React from "react";
import Header from "./Header";

const MainMenu = () => {
    return (
        <div className="page-container">
            <Header />
            <h2>MAIN MENU</h2>
        </div>
    );
};

export default MainMenu;